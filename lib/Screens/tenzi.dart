import 'package:flutter/material.dart';

class Tenzi extends StatelessWidget {
  const Tenzi({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
          backgroundColor: Colors.blue,
          title: const Text('Tenzi Za Rohoni'),
          centerTitle: true),

    );

  }
}