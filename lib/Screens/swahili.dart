import 'package:flutter/material.dart';

class Swahili extends StatelessWidget {
  const Swahili({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
          backgroundColor: Colors.blue,
          title: const Text('Bibilia'),
          centerTitle: true),

    );

  }
}